# 2026-06-01 — Full provider/feature parity vs 3 repos

## Task
User: ensure XLab has 100% of features + providers from OmniRoute, 9router,
CLIProxyAPI; complete to international standard; make most detailed checklist.

## Key findings
- 9router provider registry == XLab's (XLab is direct descendant). 100% parity.
- CLIProxyAPI providers (gemini/vertex/claude/codex/kimi/xai/antigravity/
  aistudio/openai-compat) all present except aistudio-WS + xai-OAuth "Grok Build".
  Its distinctive arch features (Go SDK, Postgres/Git/S3 store, signature cache,
  payload rules, WS gateway, cloaking) — XLab has equivalents except: payload
  rules engine, WS gateway, Postgres/Git/S3 store, Amp CLI routes.
- DOMINANT GAP = OmniRoute's ~180 providers vs XLab's ~75. Fetched OmniRoute's
  real providers.ts (89KB) from GitHub raw — authoritative source.

## What I did (Đợt 1 — committed)
3-file wiring per provider: src/shared/constants/providers.js (UI registry) +
open-sse/config/providers.js (baseUrl/format) + open-sse/services/model.js
(ALIAS_TO_PROVIDER_ID).
- Surfaced ~30 OmniRoute providers already in backend but commented out in UI.
- Added ~80 new OpenAI-compatible API-key providers + Chinese LLMs.
- Added 9 enterprise/cloud (azure-ai, watsonx, oci, sap, databricks, datarobot,
  clarifai, snowflake, heroku) with hasProviderSpecificData.
- Added 9 local self-hosted (lm-studio, vllm, lemonade, llamafile, llama-cpp,
  triton, docker-model-runner, xinference, oobabooga).
- AI_PROVIDERS: 75 -> 198. Backend PROVIDERS: -> 170.
- All passthroughModels:true, route via generic default executor + default
  validate probe. Verified parseModel resolves new aliases+ids. Build + 531
  tests pass. Deployed to 157.66.100.194:1212 (/api/version=200, models=200).
- Created docs/FEATURE-PARITY-CHECKLIST.md (whitelisted in .gitignore since
  docs/* ignored). Source of truth for remaining tiers.

## Remaining (documented in checklist, NOT done — need bespoke executors)
- Đợt 2: bedrock (SigV4); image/video/music providers (ideogram, suno, udio,
  haiper, leonardo).
- Đợt 3: OAuth/CLI vendor (qoder, agy, amazon-q, gitlab-duo, zed, trae,
  windsurf, devin-cli, aistudio-WS, xai-OAuth) — each needs OAuth flow+executor.
- Đợt 4: web-cookie scrapers (~19: chatgpt-web, gemini-web, claude-web, etc.) —
  ToS-risky, each needs reverse-engineered executor. cloud agents (jules,
  devin, codex-cloud). upstream-proxy (cliproxyapi, 9router, auto).
- Đợt 5: payload rules engine, WS gateway /v1/ws, Amp CLI /api/provider/*,
  Postgres/Git/S3 credential store, gamification, auto-combo optimizer.

## ⚠️ Workspace automation keeps auto-committing + auto-pushing
Same as 2026-05-31: a background process bundled my 3 provider files into a
commit titled "feat(usage): import and merge Cockpit usage exports" (39500c49)
— wrong message, mixed with non-mine cockpit work. No git hooks; it's external
automation. My provider work IS committed (just mislabeled). Checklist committed
cleanly as ea07efaf. Flag to user that commits/pushes happen automatically.
