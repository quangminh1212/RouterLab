/** Provider module: command-code (Omni gap-fill) */
export const id = "command-code";
/** Alias of `commandcode` — same endpoint for Omni naming parity. */
export default {
  baseUrl: "https://api.commandcode.ai/alpha/generate",
  format: "commandcode",
  aliasOf: "commandcode",
};
